volumeexec is a daemon that serves CRI ExecService API.

It is used by kubelet to execute mount utilities (/sbin/mount.nfs,
/sbin/mkfs.xfs, ...) in containers and not on the host.

TODO: link to a proposal.
